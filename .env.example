SECRET_KEY=
DEBUG=True

# Credentials: Override if not using defaults
DB_NAME=jobplatform
DB_USER=jobplatform
DB_PASSWORD=jobplatform
MINIO_USER=jobplatform
MINIO_PASSWORD=jobplatform
MINIO_BUCKET=jobplatform

# MinIO: Public endpoint for files served to users
MINIO_PUBLIC_ENDPOINT=localhost:9000

# Livekit credentials
LIVEKIT_URL=ws://localhost:7880
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# AI Settings
# AI Backend: "local" for local AI, "gemini" for production
# AI_GPU_VENDOR: "nvidia", "amd" or "cpu" doesnt matter unless you use local ai for the server
AI_BACKEND=gemini
AI_GPU_VENDOR=cpu

# AI Model: AMD/CPU users should pick a .gguf model, Nvidia users should pick an ollama model
AI_LOCAL_MODEL=qwen2.5-3b-instruct-q4_k_m.gguf
AI_GEMINI_MODEL=gemma-4-31b-it

# Gemini API key (only when using AI_BACKEND=gemini)
GEMINI_API_KEY=
